"""console-v2: the sidebar launcher.

Everything in this package is additive. It imports the frozen engine
(catalog / nodes / runner / report) and never modifies it. The v1 page
(web/index.html + web/builder.js) and the report generator
(engine/report.py) are byte-identical to tag ui-v1-baseline.
"""
